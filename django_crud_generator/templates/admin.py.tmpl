

class ${model_name}Admin(admin.ModelAdmin):
    list_filter = []
    search_fields = (
        'id',
    )
    inlines = ${list_inline_classes}
    list_display = ${list_display}

admin.site.register(${model_name}, ${model_name}Admin)
